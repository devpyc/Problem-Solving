#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

    int n,k;
    cin>>n>>k;
    while (n--) {
        int a,b;
        cin>>a>>b;
    }
    cout<<"비와이";
}