#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

    string s;
    int cnt=0;
    while (getline(cin,s)) {
        cnt++;
    }
    cout<<cnt;
}