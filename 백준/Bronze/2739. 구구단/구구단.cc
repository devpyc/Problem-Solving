#include <bits/stdc++.h>
#define f() for(int i=1; i<=9; i++)
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin>>n;
    f() {
        cout<<n<<" * "<<i<<" = "<<n*i<<"\n";
    }
}