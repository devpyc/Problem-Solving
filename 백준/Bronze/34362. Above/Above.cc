#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

    long double n;
    cin>>n;
    n-=0.3;
    cout.precision(4);
    cout<<fixed;
    cout<<n;
}