#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

	int n;
	cin>>n;

	vector<long long>ans(n+1);

	ans[0]=1;
	for(int i=1; i<=n; i++){
		ans[i]=(ans[i-1]%1000000007*i)%1000000007;
	}
	cout<<ans[n];
}